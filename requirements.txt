# Handsome Agent Dependencies
# Hermes-Brain + OpenClaw-Body Architecture

# Core dependencies
pydantic>=2.0.0
pydantic-settings>=2.0.0
cachetools>=5.3.0
httpx>=0.25.0
psutil>=5.9.0

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Terminal UI - inquirer (跨平台，推荐)
inquirer>=3.0.0

# Rich library for enhanced terminal output (可选)
rich>=13.0.0

# Database
# sqlite3 (built-in)

# Testing dependencies
pytest>=6.0.0
pytest-asyncio>=0.14.0
pytest-cov>=2.10.0

# LLM Integration (optional - uncomment when needed)
# openai>=1.0.0
# anthropic>=0.10.0
# langchain>=0.1.0

# Advanced features (optional)
# tinydb>=4.7.0          # Lightweight JSON database
# structlog>=24.0.0     # Structured logging
# chromadb>=0.4.0         # Vector database for RAG
# faiss-cpu>=1.7.0        # Similarity search
# sentence-transformers>=2.2.0  # Embedding models
